export function getIndicator(grid: number[][], keyLocation: { x: number, y: number }): { x: number, y: number } {
  return { x: 0, y: 0 }
}

export function getGuess(grid: number[][]): { x: number, y: number } {
  return { x: 0, y: 0 }
}